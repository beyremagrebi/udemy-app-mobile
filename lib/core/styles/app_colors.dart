import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xFF4B008B);
  static const Color secondaryColor = Color(0xFF7E2FCB);

  static const Color accentColor = Color(0xFF7E2FCB);

  static const Color surface = Colors.white;

  // Surface gradient base colors
  static const Color surfaceGradientStart = Color(0xFF4B008B);
  static const Color surfaceGradientEnd = Color(0xFF7E2FCB);

  static const Color bottomNabigationColor = Color.fromARGB(255, 147, 43, 250);

  // Text colors
  static const Color onPrimary = Colors.white;
  static const Color onSecondary = Colors.white;
  static const Color onBackground = Colors.white;
  static const Color onSurface = Colors.black;

  // Error color
  static const Color error = Colors.red;
  static const Color onError = Colors.white;
}
